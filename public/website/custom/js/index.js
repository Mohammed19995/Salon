vm_product = new Vue({
    el : '#vm_index',
    data : {

        sliders : sliders,
        search : search,
        category : category,
        brands : brands,
        order_by_data : order_by_data,
        limit_numbers_data : limit_numbers_data
    }
});
